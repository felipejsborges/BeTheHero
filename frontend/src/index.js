import React from 'react';
import ReactDOM from 'react-dom'; //o ReactDOM serve pro React interagir com a DOM
// import './index.css'; - //linha removida por termos excluído o index.css
import App from './App';
// import * as serviceWorker from './serviceWorker'; - //linha removida por termos excluído o serviceWorker

ReactDOM.render(  
  <App />,
  document.getElementById('root')
);

// aqui tinha um comentário que foi excluído por termos excluído o service worker
